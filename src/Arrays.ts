let numbers: number[]=[1,2,3];
//  if you use empty array, always use annotation. because type will be any if array is empty.
// compiler can still infer the type of the array.
// intellisense to show all form of  methods  that can be applied.
// shows error when type error.
// numbers.forEach(n=> n.)
// after the period it uses intellisense to know what methods are applicable for the said type.